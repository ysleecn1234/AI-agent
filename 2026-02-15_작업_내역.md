# 2026년 2월 15일 작업 내역

## ✅ 완료된 작업

### 1. 채팅 답변 마크다운 스타일링 개선 ⭐
**커밋**: `0aa9119`, `1e604d9`
- **문제**: 채팅 AI 답변의 가독성이 떨어짐
- **해결**:
  - `react-markdown` 라이브러리 사용
  - 제목, 리스트, 코드 블록, 인용구 등 상세 스타일링 적용
  - 과도한 스타일링 제거 후 자연스럽게 재조정
- **파일**: `frontend/app/chat/page.tsx`

### 2. 드라이브 파일 업로드 필수 필드 추가
**커밋**: `fc5adbf`
- **문제**: 422 에러 - `creator_id`, `creator_department` 필드 누락
- **해결**: 
  - `frontend/lib/api.ts`의 `uploadDocument` 메서드에 필수 필드 추가
  - localStorage에서 사용자 정보 읽어서 FormData에 포함
- **파일**: `frontend/lib/api.ts`

### 3. 하드코딩된 localhost 주소 수정
**커밋**: `8fc124d`
- **문제**: 서버 배포 시 localhost로 API 호출하여 실패
- **해결**: 
  - `process.env.NEXT_PUBLIC_API_URL` 또는 서버 IP 사용
  - `frontend/app/drive/documents/[id]/page.tsx`의 하드코딩 제거
- **파일**: `frontend/app/drive/documents/[id]/page.tsx`

### 4. 문서 미리보기 인증 문제 해결
**커밋**: `52db79a`
- **문제**: iframe에서 401 Unauthorized 에러 (iframe은 Authorization 헤더 전송 불가)
- **해결**:
  - fetch로 파일을 Blob으로 다운로드
  - `URL.createObjectURL()`로 Blob URL 생성
  - iframe src에 Blob URL 설정
- **파일**: `frontend/app/drive/documents/[id]/page.tsx`

### 5. 다중 기능 추가
**커밋**: `74e7eb9`
- **Drive 다운로드 버튼**:
  - 문서 목록의 `...` 메뉴에 "다운로드" 옵션 추가
  - `GET /drive/documents/{doc_id}/file?download=true` API 호출
  - Blob 다운로드 및 자동 파일명 설정
- **문서 미리보기 iframe**:
  - 문서 상세 페이지에 iframe 추가
  - Blob URL 방식으로 인증 문제 해결
- **에이전트 실시간 추천**:
  - 채팅 입력창에 500ms 디바운스 적용
  - 3자 이상 입력 시 `api.recommendAgents()` 자동 호출
  - 추천 에이전트 카드 UI 표시
- **파일**: `frontend/app/drive/page.tsx`, `frontend/app/drive/documents/[id]/page.tsx`, `frontend/app/chat/page.tsx`

### 6. 토큰 만료 시 자동 로그아웃
**커밋**: `bdaa27b`
- **문제**: 401 에러 발생 시 사용자가 수동으로 로그아웃해야 함
- **해결**:
  - `frontend/lib/api.ts`의 `request` 메서드에 401 처리 로직 추가
  - 토큰 삭제 후 `/auth/login`으로 자동 리다이렉트
- **파일**: `frontend/lib/api.ts`

### 7. 마크다운 렌더링 및 에이전트 생성 API 수정
**커밋**: `62f8bd7`
- **마크다운 렌더링**:
  - `react-markdown` 라이브러리 적용
  - AI 답변에만 마크다운 렌더링, 사용자 메시지는 일반 텍스트
- **에이전트 생성 API**:
  - 백엔드 다단계 API에 맞춰 프론트엔드 수정
  - `/agents/draft` → `/agents/draft/step1` → `/agents/draft/step2` → `/agents/publish`
  - `selected_messages` 기반 초안 생성
- **파일**: `frontend/app/chat/page.tsx`, `frontend/app/agents/create/page.tsx`, `frontend/lib/api.ts`

### 8. ApiClient 토큰 갱신 로직 수정
**커밋**: `fb19e2c`
- **문제**: 토큰이 업데이트되어도 이전 토큰으로 요청
- **해결**:
  - 생성자에서 토큰을 저장하지 않고, 매 요청마다 localStorage에서 읽기
  - 최신 토큰으로 항상 요청 보장
- **파일**: `frontend/lib/api.ts`

### 9. 채팅 session_id 처리 및 대화 이어가기
**커밋**: `9bf9ee2`
- **기능**: 
  - 백엔드에서 반환하는 `session_id`를 저장
  - 다음 메시지 전송 시 `session_id` 포함하여 대화 컨텍스트 유지
- **파일**: `frontend/app/chat/page.tsx`

### 10. 백엔드 전체 로직 점검 및 드라이브 기능 보강
**커밋**: `2e8903f`
- 백엔드 API 엔드포인트 확인 및 프론트엔드 연동 검증
- 드라이브 관련 API 호출 로직 보강

---

## 📊 오늘의 통계
- **총 커밋 수**: 10개
- **수정된 주요 파일**: 
  - `frontend/app/chat/page.tsx`
  - `frontend/app/drive/page.tsx`
  - `frontend/app/drive/documents/[id]/page.tsx`
  - `frontend/lib/api.ts`
  - `frontend/app/agents/create/page.tsx`
- **해결한 이슈**: 8개
- **추가한 기능**: 5개

---

## 🐛 발견된 백엔드 버그 (테스트 중 확인)
1. **에이전트 발행 실패**: `TypeError: 'parameters' is an invalid keyword argument for Agent`
2. **드라이브 목록 조회 실패**: `PostgresClient.list_documents() got an unexpected keyword argument 'department'`
3. **드라이브 업로드 실패**: 422/500 에러
4. **Milvus 검색 실패**: `Unsupported field type: 0`

---

## ✨ 주요 성과
1. **사용자 경험 대폭 개선**: 마크다운 렌더링으로 AI 답변 가독성 향상
2. **보안 강화**: 토큰 만료 자동 처리
3. **편의성 증대**: 드라이브 다운로드, 미리보기, 실시간 에이전트 추천
4. **안정성 향상**: API 클라이언트 토큰 관리 개선

---

## 🎯 남은 작업
**없음** - 모든 프론트엔드 작업 완료!

백엔드 버그 수정 대기 중입니다.
